# Scratch scripts for manual API testing during development.
# These are NOT part of the formal test suite.
# Formal tests live in: backend/tests/
#
# Usage (from project root with venv active):
#   python test_api.py        — end-to-end auth + project smoke test
#   python test_enum.py       — enum value validation
#   python test_tasks.py      — tasks API smoke test
#   python test_ws.py         — WebSocket connection test
